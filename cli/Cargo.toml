[package]
name = "anchor-cli"
version = "0.31.1"
edition = "2021"
description = "Anchor CLI for Solana development"

[[bin]]
name = "anchor"
path = "src/main.rs"

[build-dependencies]
reqwest = { version = "0.11", features = ["blocking", "json"] }
